@model SFA.Apprenticeships.Web.Manage.ViewModels.ProviderUser.VerifyEmailViewModel

@using SFA.Apprenticeships.Web.Manage.Constants
@{
    ViewBag.Title = "Manage an Apprentice - Vertify Email";
}

<h1 id="accountCreated" class="heading-xlarge">Verify email</h1>

<p class="text">
    An email has been sent to <b class="">@Model.EmailAddress</b>.
    It’ll contain a 6-character code that you’ll need to verify your email
    address.
</p>

@using (Html.BeginRouteForm(ManagementRouteNames.VerifyEmail, FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.Partial("ValidationSummary", ViewData.ModelState)

    @Html.FormTextFor(
            m => m.VerificationCode,
            controlHtmlAttributes: new { type = "text", autofocus = "autofocus", spellcheck = "false", @class = "form-control-1-3" })
    
    <p><a href="@Url.RouteUrl(ManagementRouteNames.ResendVerificationCode)">Resend code</a></p>
    
    <p><a href="@Url.RouteUrl(ManagementRouteNames.Settings)">Change email</a></p>

    <div class="form-group">
        <button class="button activate-page-btn" id="btnSignIn">Verify email</button>
    </div>

}